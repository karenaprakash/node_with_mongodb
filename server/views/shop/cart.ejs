<%- include('../components/head.ejs') %>
</head>
<body>
    <%- include('../components/navigation.ejs') %>
    <h1>Cart</h1>
    <% if( products.length > 0 ) { %>
        
        <div class="grid">
                <% for (let product of products) { %>
                    <article class="card product-item">
                        <header class="card__header">
                            <h1 class="product__title"><%= product.productData.name %></h1>
                        </header>
                        <div class="card__image">
                            <img src="<%= product.productData.imageUrl %>"
                                alt="<%= product.productData.name %>">
                        </div>
                        <div class="card__content">
                            <h2 class="product__price">Price: $<%= product.productData.price %></h2>
                            <h4>Quantity: <%= product.qty %></h4>
                            <p class="product__description"><%= product.productData.description %></p>
                        </div>
                        <div class="card__actions">
                             <a href="/products/<%= product.productData.id %>" class="edit_btn">Details</a>
                             <form class="addOrEditForm" method="POST" action="/cart-delete-item">
                                <input type="hidden" name="productId" value="<%= product.productData.id %>">
                                <input type="hidden" name="productPrice" value="<%= product.productData.price %>">
                                <button class="btn" type="submit">Delete</button>
                            </form>
                            <form class="addOrEditForm" method="POST" action="/cart">
                                <input type="hidden" name="productId" value="<%= product.productData.id %>">
                                <button class="btn" type="submit">Add to Cart </button>
                            </form>
                        </div>
                    </article>
                <% } %>
            </div>

    <% } else {  %>
        <h1> No Products in the cart. </h1>
    <% }  %>

<%- include('../components/end.ejs') %>